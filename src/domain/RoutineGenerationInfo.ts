export interface IRoutineGenerationInfo {
    baseRoutineId: string,
    startingDate: Date,
    addDeloadWeek: boolean,
    squatMax: number,
    deadliftMax: number,
    benchPressMax: number,
    overHeadPressMax: number,
}
